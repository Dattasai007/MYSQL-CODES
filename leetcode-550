with fl as(select player_id, min(event_date) as fd
from activity
group by player_id)
select round(count(distinct case when datediff(event_date, fd)=1 then a.player_id end)/(select count(*) from fl), 2) as fraction
from activity a join fl
on a.player_id=fl.player_id
