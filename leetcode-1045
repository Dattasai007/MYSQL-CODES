with cte as(select customer_id, count(distinct product_key) as np
from customer
group by customer_id)
select customer_id 
from cte 
where np=(
    select count(distinct product_key)
    from product
)
